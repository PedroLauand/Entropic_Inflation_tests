Legacy Lucas/Gurobi runtime files live here.

This folder contains the older feasibility-style Shannon scripts that depend on
`gurobipy` directly, plus wrappers remain at their historical paths for
backward compatibility.
